<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>PayBySquare</RootNamespace>
    <AssemblyName>PayBySquare</AssemblyName>
  </PropertyGroup>

  <!-- No PackageReferences: the generator is 100% dependency-free.
       Tests live in ../test (PayBySquare.Tests), which may use libraries
       for independent verification (LZMA-SDK as reference decoder). -->

</Project>
